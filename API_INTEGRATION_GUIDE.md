# TAS 系统 API# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System)# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
-# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.p# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.561704# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
-# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        //# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST /api/auth/register`

**# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST /api/auth/register`

**请求体**：
```json
{
  "username": "string",      //# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST /api/auth/register`

**请求体**：
```json
{
  "username": "string",      // 3-20位字母、数字或下划线
  "email": "string",# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST /api/auth/register`

**请求体**：
```json
{
  "username": "string",      // 3-20位字母、数字或下划线
  "email": "string",         // 有效的邮箱格式
  "realName# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST /api/auth/register`

**请求体**：
```json
{
  "username": "string",      // 3-20位字母、数字或下划线
  "email": "string",         // 有效的邮箱格式
  "realName": "string",      // 真实姓名
# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST /api/auth/register`

**请求体**：
```json
{
  "username": "string",      // 3-20位字母、数字或下划线
  "email": "string",         // 有效的邮箱格式
  "realName": "string",      // 真实姓名
  "password": "string",      //# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST /api/auth/register`

**请求体**：
```json
{
  "username": "string",      // 3-20位字母、数字或下划线
  "email": "string",         // 有效的邮箱格式
  "realName": "string",      // 真实姓名
  "password": "string",      // 至少6位
  "role": "student# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST /api/auth/register`

**请求体**：
```json
{
  "username": "string",      // 3-20位字母、数字或下划线
  "email": "string",         // 有效的邮箱格式
  "realName": "string",      // 真实姓名
  "password": "string",      // 至少6位
  "role": "student" | "teacher" | "admin"# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST /api/auth/register`

**请求体**：
```json
{
  "username": "string",      // 3-20位字母、数字或下划线
  "email": "string",         // 有效的邮箱格式
  "realName": "string",      // 真实姓名
  "password": "string",      // 至少6位
  "role": "student" | "teacher" | "admin"  // 角色
}
```

# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST /api/auth/register`

**请求体**：
```json
{
  "username": "string",      // 3-20位字母、数字或下划线
  "email": "string",         // 有效的邮箱格式
  "realName": "string",      // 真实姓名
  "password": "string",      // 至少6位
  "role": "student" | "teacher" | "admin"  // 角色
}
```

**注意**：
- 系统初始化# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST /api/auth/register`

**请求体**：
```json
{
  "username": "string",      // 3-20位字母、数字或下划线
  "email": "string",         // 有效的邮箱格式
  "realName": "string",      // 真实姓名
  "password": "string",      // 至少6位
  "role": "student" | "teacher" | "admin"  // 角色
}
```

**注意**：
- 系统初始化时，任何人可以注册管理员
- 系统# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST /api/auth/register`

**请求体**：
```json
{
  "username": "string",      // 3-20位字母、数字或下划线
  "email": "string",         // 有效的邮箱格式
  "realName": "string",      // 真实姓名
  "password": "string",      // 至少6位
  "role": "student" | "teacher" | "admin"  // 角色
}
```

**注意**：
- 系统初始化时，任何人可以注册管理员
- 系统有管理员后，只有管理员可以创建新管理员# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST /api/auth/register`

**请求体**：
```json
{
  "username": "string",      // 3-20位字母、数字或下划线
  "email": "string",         // 有效的邮箱格式
  "realName": "string",      // 真实姓名
  "password": "string",      // 至少6位
  "role": "student" | "teacher" | "admin"  // 角色
}
```

**注意**：
- 系统初始化时，任何人可以注册管理员
- 系统有管理员后，只有管理员可以创建新管理员

**响应**：
```json
{
  "success": true,
# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST /api/auth/register`

**请求体**：
```json
{
  "username": "string",      // 3-20位字母、数字或下划线
  "email": "string",         // 有效的邮箱格式
  "realName": "string",      // 真实姓名
  "password": "string",      // 至少6位
  "role": "student" | "teacher" | "admin"  // 角色
}
```

**注意**：
- 系统初始化时，任何人可以注册管理员
- 系统有管理员后，只有管理员可以创建新管理员

**响应**：
```json
{
  "success": true,
  "data": {
    "token":# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST /api/auth/register`

**请求体**：
```json
{
  "username": "string",      // 3-20位字母、数字或下划线
  "email": "string",         // 有效的邮箱格式
  "realName": "string",      // 真实姓名
  "password": "string",      // 至少6位
  "role": "student" | "teacher" | "admin"  // 角色
}
```

**注意**：
- 系统初始化时，任何人可以注册管理员
- 系统有管理员后，只有管理员可以创建新管理员

**响应**：
```json
{
  "success": true,
  "data": {
    "token": "string",  // JWT token
# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST /api/auth/register`

**请求体**：
```json
{
  "username": "string",      // 3-20位字母、数字或下划线
  "email": "string",         // 有效的邮箱格式
  "realName": "string",      // 真实姓名
  "password": "string",      // 至少6位
  "role": "student" | "teacher" | "admin"  // 角色
}
```

**注意**：
- 系统初始化时，任何人可以注册管理员
- 系统有管理员后，只有管理员可以创建新管理员

**响应**：
```json
{
  "success": true,
  "data": {
    "token": "string",  // JWT token
    "user": {
      "id": "# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST /api/auth/register`

**请求体**：
```json
{
  "username": "string",      // 3-20位字母、数字或下划线
  "email": "string",         // 有效的邮箱格式
  "realName": "string",      // 真实姓名
  "password": "string",      // 至少6位
  "role": "student" | "teacher" | "admin"  // 角色
}
```

**注意**：
- 系统初始化时，任何人可以注册管理员
- 系统有管理员后，只有管理员可以创建新管理员

**响应**：
```json
{
  "success": true,
  "data": {
    "token": "string",  // JWT token
    "user": {
      "id": "string",
      "username": "string",
      "email": "string",# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST /api/auth/register`

**请求体**：
```json
{
  "username": "string",      // 3-20位字母、数字或下划线
  "email": "string",         // 有效的邮箱格式
  "realName": "string",      // 真实姓名
  "password": "string",      // 至少6位
  "role": "student" | "teacher" | "admin"  // 角色
}
```

**注意**：
- 系统初始化时，任何人可以注册管理员
- 系统有管理员后，只有管理员可以创建新管理员

**响应**：
```json
{
  "success": true,
  "data": {
    "token": "string",  // JWT token
    "user": {
      "id": "string",
      "username": "string",
      "email": "string",
      "realName": "string",# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST /api/auth/register`

**请求体**：
```json
{
  "username": "string",      // 3-20位字母、数字或下划线
  "email": "string",         // 有效的邮箱格式
  "realName": "string",      // 真实姓名
  "password": "string",      // 至少6位
  "role": "student" | "teacher" | "admin"  // 角色
}
```

**注意**：
- 系统初始化时，任何人可以注册管理员
- 系统有管理员后，只有管理员可以创建新管理员

**响应**：
```json
{
  "success": true,
  "data": {
    "token": "string",  // JWT token
    "user": {
      "id": "string",
      "username": "string",
      "email": "string",
      "realName": "string",
      "role": "string",
      "avatarUrl": null,
# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST /api/auth/register`

**请求体**：
```json
{
  "username": "string",      // 3-20位字母、数字或下划线
  "email": "string",         // 有效的邮箱格式
  "realName": "string",      // 真实姓名
  "password": "string",      // 至少6位
  "role": "student" | "teacher" | "admin"  // 角色
}
```

**注意**：
- 系统初始化时，任何人可以注册管理员
- 系统有管理员后，只有管理员可以创建新管理员

**响应**：
```json
{
  "success": true,
  "data": {
    "token": "string",  // JWT token
    "user": {
      "id": "string",
      "username": "string",
      "email": "string",
      "realName": "string",
      "role": "string",
      "avatarUrl": null,
      "status": "active",
      "# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST /api/auth/register`

**请求体**：
```json
{
  "username": "string",      // 3-20位字母、数字或下划线
  "email": "string",         // 有效的邮箱格式
  "realName": "string",      // 真实姓名
  "password": "string",      // 至少6位
  "role": "student" | "teacher" | "admin"  // 角色
}
```

**注意**：
- 系统初始化时，任何人可以注册管理员
- 系统有管理员后，只有管理员可以创建新管理员

**响应**：
```json
{
  "success": true,
  "data": {
    "token": "string",  // JWT token
    "user": {
      "id": "string",
      "username": "string",
      "email": "string",
      "realName": "string",
      "role": "string",
      "avatarUrl": null,
      "status": "active",
      "createdAt": number,
      "updatedAt# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST /api/auth/register`

**请求体**：
```json
{
  "username": "string",      // 3-20位字母、数字或下划线
  "email": "string",         // 有效的邮箱格式
  "realName": "string",      // 真实姓名
  "password": "string",      // 至少6位
  "role": "student" | "teacher" | "admin"  // 角色
}
```

**注意**：
- 系统初始化时，任何人可以注册管理员
- 系统有管理员后，只有管理员可以创建新管理员

**响应**：
```json
{
  "success": true,
  "data": {
    "token": "string",  // JWT token
    "user": {
      "id": "string",
      "username": "string",
      "email": "string",
      "realName": "string",
      "role": "string",
      "avatarUrl": null,
      "status": "active",
      "createdAt": number,
      "updatedAt": number
    }
  }
}
```

### 3.2# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST /api/auth/register`

**请求体**：
```json
{
  "username": "string",      // 3-20位字母、数字或下划线
  "email": "string",         // 有效的邮箱格式
  "realName": "string",      // 真实姓名
  "password": "string",      // 至少6位
  "role": "student" | "teacher" | "admin"  // 角色
}
```

**注意**：
- 系统初始化时，任何人可以注册管理员
- 系统有管理员后，只有管理员可以创建新管理员

**响应**：
```json
{
  "success": true,
  "data": {
    "token": "string",  // JWT token
    "user": {
      "id": "string",
      "username": "string",
      "email": "string",
      "realName": "string",
      "role": "string",
      "avatarUrl": null,
      "status": "active",
      "createdAt": number,
      "updatedAt": number
    }
  }
}
```

### 3.2 登录
**端点**：`POST /# TAS 系统 API 集成指南

## 1. 系统概述

TAS (Teaching Auth System) 是一个教学管理通用用户管理系统，提供完整的用户认证、班级管理和应用授权功能。本指南详细说明如何通过 API 集成 TAS 系统。

## 2. API 基础信息

### 2.1 基础 URL
- **前端地址**：https://caolg.pages.dev
- **后端 API**：https://tas-worker.56170496.workers.dev

### 2.2 认证方式
- 使用 JWT (JSON Web Token) 进行认证
- 所有受保护的 API 端点需要在请求头中包含 `Authorization: Bearer <token>`

### 2.3 响应格式
所有 API 响应采用统一格式：

```json
{
  "success": boolean,
  "data": any,           // 成功时返回的数据
  "error": string,        // 失败时返回的错误信息
  "message": string       // 成功时返回的消息
}
```

## 3. 认证 API

### 3.1 注册
**端点**：`POST /api/auth/register`

**请求体**：
```json
{
  "username": "string",      // 3-20位字母、数字或下划线
  "email": "string",         // 有效的邮箱格式
  "realName": "string",      // 真实姓名
  "password": "string",      // 至少6位
  "role": "student" | "teacher" | "admin"  // 角色
}
```

**注意**：
- 系统初始化时，任何人可以注册管理员
- 系统有管理员后，只有管理员可以创建新管理员

**响应**：
```json
{
  "success": true,
  "data": {
    "token": "string",  // JWT token
    "user": {
      "id": "string",
      "username": "string",
      "email": "string",
      "realName": "string",
      "role": "string",
      "avatarUrl": null,
      "status": "active",
      "createdAt": number,
      "updatedAt": number
    }
  }
}
```

### 3.2 登录
**端点**：`POST /api/auth/login`

**请求体**：
```json
{
